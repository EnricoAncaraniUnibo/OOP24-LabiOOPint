package labioopint.model;

public class SwapPositionPowerUp extends PowerUpImpl {
    @Override
    public void activate() {
        if (isCollected()) {
            // Logica specifica per scambiare posizione con un altro giocatore
        }
    }
}