package labioopint.controller.api;

public interface GameController {

    /**
     * The method do the action that the user want to do
     * 
     * @param action the action selected by the user
     * 
     */
    void action(Object action);
}
